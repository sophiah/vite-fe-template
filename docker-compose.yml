services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: frontend
    environment:
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - ./:/workspace
      - frontend_node_modules:/workspace/frontend/node_modules

  postgres:
    image: postgres:16-alpine
    container_name: postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: app
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./sql:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U app -d app"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: backend
    env_file:
      - path: ./.secret
        required: false
      - path: ./backend/.secret
        required: false
    environment:
      - DATABASE_URL=postgresql://app:app@postgres:5432/app
      - APP_AUTH_SECRET=dev-self-signed-secret
      - COOKIE_DOMAIN=localhost
      - COOKIE_SECURE=true
      - FRONTEND_AUTH_SUCCESS_URL=https://localhost:8000/
      - FRONTEND_AUTH_ERROR_URL=https://localhost:8000/?authError=1
      - WATCHFILES_FORCE_POLLING=true
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./backend/src:/workspace

  nginx:
    image: nginx:1.27-alpine
    container_name: nginx
    depends_on:
      - frontend
      - backend
    ports:
      - "8000:8000"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/index.html:/usr/share/nginx/html/index.html:ro
      - ./nginx/assets:/usr/share/nginx/html/assets:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro

volumes:
  frontend_node_modules:
  postgres_data:
